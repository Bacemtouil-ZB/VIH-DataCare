server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    # Configuration SPA: toute route non statique retombe sur index.html.
    try_files $uri /index.html;
  }
}
