# PostgreSQL
DB_USER=postgres
DB_PASSWORD=1234
DB_NAME=vihdatacaredb
DB_PORT=5432

# Backend
PORT=3000
JWT_SECRET=change_me_with_a_long_random_secret
FRONTEND_URL=http://localhost:5173

# Frontend
FRONTEND_PORT=5173

# Optional SMTP (leave empty if not used)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_SECURE=false
